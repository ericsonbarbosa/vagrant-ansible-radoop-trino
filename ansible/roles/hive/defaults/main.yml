---
hive_version: "3.1.3"

# Instalação via url tornou-se muito lenta, optei por manter o arquivo na pasta files 
#hive_mirror: "https://archive.apache.org/dist/hive/hive-{{ hive_version }}/apache-hive-{{ hive_version }}-bin.tar.gz"

install_dir: "/opt"